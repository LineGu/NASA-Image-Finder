import styled from 'styled-components';

const TagStyler = styled.ul`
  margin: 0.5em 0 0 0;

  .hash {
    display: inline;
    margin: 0;
    padding: 0;
    color: #f5be4f;
  }
`;

export default TagStyler;
