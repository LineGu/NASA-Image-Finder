import styled from 'styled-components';

const ArrowButtonStyler = styled.div`
  svg {
    color: #000000;
  }

  svg:hover {
    cursor: pointer;
    color: #f5be4f;
  }
`;

export default ArrowButtonStyler;
